<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.mycompany.viewport_mini_web.dao.UserDao">
<insert id="insert" parameterType="User">
		<selectKey keyProperty="usid" order="BEFORE" resultType="int">
			select seq_usid.nextval from dual
		</selectKey>
		insert into
		users(usid,upassword,uname,uemail,ugender,uphonenumber,uaddress,uaddressdetail,urole)
		values(#{usid}, #{upassword},#{uname},#{uemail},#{ugender},#{uphonenumber},#{uaddress}, #{uaddressdetail},#{urole}
		)
	</insert>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.mycompany.viewport_mini_web.dao.UserDao">
	<insert id="insert" parameterType="User">
		<selectKey keyProperty="usid" order="BEFORE"
			resultType="int">
			select seq_usid.nextval from dual
		</selectKey>
		insert into
		users(usid,upassword,uname,uemail,ugender,uphonenumber,uaddress,uaddressdetail,urole,uenabled)
		values(#{usid},
		#{upassword},#{uname},#{uemail},#{ugender},#{uphonenumber},#{uaddress},
		#{uaddressdetail},#{urole},#{uenabled}
		)
	</insert>

	<select id="selectByUemail" parameterType="String"
		resultType="User">
		select usid,
		uemail,upassword,uname,ugender,uphonenumber,uaddress,uaddressdetail,urole,uenabled
		from users
		where uemail = #{uemail}
	</select>

	<select id="selectuserAll">
		select usid,
		uemail,upassword,uname,ugender,uphonenumber,uaddress,uaddressdetail,urole,uenabled
		from users
	</select>
	
	<update id="update" parameterType="User">
	update users set uname=#{uname},uemail=#{uemail},ugender=#{ugender},uphonenumber=#{uphonenumber},uaddress=#{uaddress},uaddressdetail=#{uaddressdetail},urole=#{urole}
	where usid=#{usid}
	</update>
	
	<delete id="deleteByUsid" parameterType="int">
	delete from users where usid=#{usid}
	</delete>
</mapper>
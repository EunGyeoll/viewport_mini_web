<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper
	namespace="com.mycompany.viewport_mini_web.dao.CartItemDao">

	<select id="findByCartIdAndProductId" resultType="CartItem">

		select ciid,ciqty,cipid,cicid from cartitem
	</select>

	<insert id="createCartProduct" parameterType="int">
		<selectKey keyProperty="ciid" order="BEFORE"
			resultType="int">
			select seq_ciid.nextval from dual
		</selectKey>
		insert into
		cartitem (ciid,ciqty,cipid,cicid) values
		(#{ciid},#{ciqty},#{cipid},#{cicid})
	</insert>

	<update id="updateCiqty" parameterType="int">
		update cartitem set
		ciid=#{ciid},ciqty=#{ciqty}, cipid=#{cipid}, cicid = #{cicid}
		where cipid=#{cipid} and cicid = #{cicid}
	</update>
	<select id="selectAllCartItems" parameterType="int"
		resultType="CartItem">
		select ciid,ciqty,cipid,cicid
		from cartitem
		where
		cicid=#{cicid}
	</select>
</mapper>